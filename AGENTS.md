# AGENTS.md

## Project Overview
**Dialogic Story Flow Chart** is a Godot 4.7+ project integrated with the **Dialogic** dialogue & story flow framework.

- **Engine Version**: Godot 4.7+
- **Rendering Driver**: GL Compatibility (Windows: D3D12)
- **Repository Remote**: `https://github.com/non-nattawut/Dialogic-Story-Flow-Chart.git`
- **Default Branch**: `main`

---

## Core Guidelines & Synchronization Rules

> [!IMPORTANT]
> **Rule: Read Karpathy Guidelines Before Coding**
> AI Agents MUST read and follow the [Karpathy Guidelines](file:///.agents/skills/karpathy-guidelines/SKILL.md) (`.agents/skills/karpathy-guidelines/SKILL.md`) before writing, modifying, or refactoring any code.
> Core Principles:
> 1. **Think Before Coding**: Explicitly state assumptions, surface tradeoffs, and clarify ambiguities before implementation.
> 2. **Simplicity First**: Implement the minimum code needed to solve the task. Avoid over-engineering or speculative features.
> 3. **Surgical Changes**: Touch only necessary lines. Match existing code style and clean up only self-created unused code.
> 4. **Goal-Driven Execution**: Establish clear, verifiable success criteria and test until verified.

> [!IMPORTANT]
> **Rule: Senior Game Developer OOP & Architecture Standards**
> AI Agents MUST design and structure code adhering to professional Senior Game Developer standards:
> 1. **OOP & Inheritance**: Utilize proper Object-Oriented Programming (OOP), encapsulation, modular static helpers, and inheritance.
> 2. **Modular Architecture & Clean Directories**: Avoid monolithic scripts or flat directory dumps. Organize code into clean subdirectories (`core/`, `ui/`). Keep code files concise and under ~250-300 lines.

> [!IMPORTANT]
> **Rule: Keep README.md Updated & In Sync**
> AI Agents working on this project MUST update and maintain `README.md` whenever core features, plugin installation procedures, node architectures, file formats, or usage APIs are added or modified.

> [!IMPORTANT]
> **Rule: Maintain & Keep AGENTS.md in Sync**
> AI Agents working on this project MUST keep `AGENTS.md` updated whenever significant changes occur, including:
> 1. New features, architectural design, or new core modules added.
> 2. Modifications to project directory structure or dependencies.
> 3. Changes in coding standards, GDScript guidelines, or engine configurations.
> 4. Updates to build, workflow, or Git sync instructions.

> [!IMPORTANT]
> **Rule: AGENTS.md File Size Limit (Max 300 Lines)**
> AI Agents MUST keep `AGENTS.md` concise and under 300 lines.
> If updates would cause `AGENTS.md` to exceed 300 lines, DO NOT expand this file. Recommend and extract detailed topic-specific guidelines into a new skill under `.agents/skills/<skill-name>/SKILL.md`.

---

## Project Structure

```
dialogic-story-flow-chart/
├── .agents/             # Agent skills and guidelines
│   └── skills/
│       └── karpathy-guidelines/ # Behavioral coding guidelines
├── addons/
│   ├── dialogic/        # Dialogic plugin framework
│   └── dialogic_flow_chart/ # Flow Chart Editor Addon
│       ├── core/        # Core Data Models & File I/O Managers
│       │   ├── flow_chart_file_manager.gd
│       │   ├── flow_chart_node_data.gd
│       │   ├── flow_chart_resource.gd
│       │   └── flow_chart_runner.gd
│       ├── ui/          # UI Controllers, Views & Inspectors
│       │   ├── flow_chart_editor.gd
│       │   ├── flow_chart_editor.tscn
│       │   ├── flow_chart_graph_edit.gd
│       │   ├── flow_chart_graph_ui.gd
│       │   └── flow_chart_inspector_manager.gd
│       ├── plugin.cfg   # Plugin configuration metadata
│       └── plugin.gd    # Addon entry point script
├── project.godot        # Godot project file
├── .gitignore           # Git ignore rules for Godot 4+
├── AGENTS.md            # Agent instructions & synchronization rules (this file)
└── README.md            # Project documentation
```

---

## Development & Code Standards

1. **GDScript Standards**:
   - Use static typing wherever possible (`var count: int = 0`, `func _ready() -> void:`).
   - Follow standard GDScript formatting and naming conventions (`PascalCase` for classes/nodes, `snake_case` for variables/functions, `UPPER_SNAKE_CASE` for constants).
   - Retain `.uid` files generated by Godot 4 for resource identification.
   - Maintain modular OOP structure with single responsibility principle across components.

2. **Addon Management**:
   - Treat `addons/dialogic/` as a managed plugin. Avoid editing core Dialogic plugin files directly unless creating custom extensions or modules within `addons/dialogic/Modules/`.

3. **Git & Sync Rules**:
   - All commits should follow conventional commit message formats (e.g., `feat:`, `fix:`, `docs:`, `refactor:`).
   - Ensure the working tree is clean and synced with remote `origin/main` after completing major tasks.

---

## Instructions for Agents

- **Pre-execution**: 
  1. Read [Karpathy Guidelines](file:///.agents/skills/karpathy-guidelines/SKILL.md) before writing or refactoring any code.
  2. Check current git status (`git status`) and branch state.
- **Post-execution**: Ensure code compiles/runs cleanly in Godot 4.7, update `AGENTS.md` and `README.md` if structure or patterns changed (keeping `AGENTS.md` ≤ 300 lines), stage and commit changes cleanly.
